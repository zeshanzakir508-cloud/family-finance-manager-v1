// lib/presentation/templates/enums/page_scroll_behavior.dart

/// The scroll behavior of the page.
enum PageScrollBehavior {
  /// No scrolling (content fixed).
  none,

  /// Normal scrolling.
  normal,

  /// Reverse scrolling (starts from bottom).
  reverse,

  /// Paged scrolling (page by page).
  paged,
}
