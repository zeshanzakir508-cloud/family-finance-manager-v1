// lib/presentation/widgets/animations/enums/animation_repeat.dart

/// The repeat behavior of the animation.
enum AnimationRepeat {
  /// No repeat (single animation).
  none,

  /// Repeat indefinitely.
  infinite,

  /// Repeat a specific number of times.
  count,

  /// Repeat with reverse on each iteration.
  mirror,
}
