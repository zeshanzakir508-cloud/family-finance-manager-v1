// lib/presentation/widgets/chips/enums/chip_size.dart

/// The size of the chip.
enum ChipSize {
  /// Small chip (24px height).
  small,

  /// Medium chip (32px height).
  medium,

  /// Large chip (40px height).
  large,
}
