// lib/presentation/widgets/feedback/enums/loading_size.dart

/// The size of the loading indicator.
enum LoadingSize {
  /// Small loading indicator (24px).
  small,

  /// Medium loading indicator (40px).
  medium,

  /// Large loading indicator (56px).
  large,

  /// Extra large loading indicator (72px).
  extraLarge;
}
