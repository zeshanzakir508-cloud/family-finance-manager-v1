// lib/presentation/widgets/media/enums/image_quality.dart

/// The quality level of the image.
enum ImageQuality {
  /// Low quality (fast loading).
  low,

  /// Medium quality (balanced).
  medium,

  /// High quality (best visual).
  high,

  /// Original quality (no compression).
  original,
}
