// lib/presentation/widgets/navigation/enums/navigation_label_behavior.dart

/// The label behavior of the navigation item.
enum NavigationLabelBehavior {
  /// Always show labels.
  alwaysShow,

  /// Show labels when selected only.
  showSelected,

  /// Never show labels (icons only).
  neverShow,
}
