// lib/presentation/widgets/selection/enums/selection_mode.dart

/// The selection mode of the selection group.
enum SelectionMode {
  /// Single selection (radio behavior).
  single,

  /// Multiple selection (checkbox behavior).
  multiple,

  /// Toggle selection (switch behavior).
  toggle,
}
