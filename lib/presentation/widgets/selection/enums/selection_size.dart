// lib/presentation/widgets/selection/enums/selection_size.dart

/// The size of the selection widget.
enum SelectionSize {
  /// Small selection (16px).
  small,

  /// Medium selection (20px).
  medium,

  /// Large selection (24px).
  large,

  /// Extra large selection (28px).
  extraLarge;
}
