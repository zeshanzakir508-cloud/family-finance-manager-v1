// lib/presentation/widgets/selection/enums/selection_state.dart

/// The state of the selection widget.
enum SelectionState {
  /// Unselected state.
  unselected,

  /// Selected state.
  selected,

  /// Indeterminate state (partial selection).
  indeterminate,

  /// Disabled state.
  disabled,
}
