// lib/presentation/widgets/tiles/enums/tile_size.dart

/// The size of the tile.
enum TileSize {
  /// Compact tile with minimal padding (48px height).
  compact,

  /// Medium tile with standard padding (56px height).
  medium,

  /// Large tile with generous padding (72px height).
  large,
}
